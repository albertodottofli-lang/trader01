'use client'; import { LineChart, Line, ResponsiveContainer } from 'recharts'; export default function StockCard({data}){ if(!data) return null; const price=Number(data.price||0); const open=Number(data.open||price); const pct = open ? ((price-open)/open*100).toFixed(2) : '0.00'; const rsi = data.rsi ?? '—'; const series = (data.series || []).map((v,i)=>({x:i,y:v})); return (<div className='card p-4 rounded-lg shadow-md m-2'><div className='flex justify-between'><div><div className='text-sm small-muted'>{data.symbol}</div><div className='text-lg font-semibold'>{data.name||data.symbol}</div></div><div className='text-right'><div className='text-xl font-bold'>${price.toFixed(2)}</div><div className={Number(pct)>=0?'text-green-400':'text-red-400'}>{pct}%</div></div></div><div className='mt-3 flex items-center justify-between'><div className='text-sm'>RSI: {rsi}</div><div style={{width:180,height:72}}><ResponsiveContainer width='100%' height='100%'><LineChart data={series}><Line type='monotone' dataKey='y' dot={false} stroke='#60a5fa' strokeWidth={2} /></LineChart></ResponsiveContainer></div></div></div>); }