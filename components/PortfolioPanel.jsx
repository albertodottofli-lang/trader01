'use client'; import {useEffect,useState} from 'react'; export default function PortfolioPanel(){ const [portfolio,setPortfolio]=useState(null); async function load(){ const r=await fetch('/api/portfolio'); setPortfolio(await r.json()); } useEffect(()=>{ load(); const i=setInterval(load,5000); return ()=>clearInterval(i); },[]); if(!portfolio) return <div className='card p-4'>Loading...</div>; return (<div className='card p-4 rounded-lg'><h3 className='font-semibold mb-2'>Portfolio</h3><div>Cash: ${portfolio.cash.toFixed(2)}</div><div className='mt-2'>Positions: {portfolio.positions.length}</div></div>); }